{
  "d6c1258159389ebaea3015eb4ff9de52d69d980f0304f2f4b56c2a0a949598fe": "#include <memory>   // For std::unique_ptr\n#include <string>   // For std::string\n#include <cstring>  // For strlen, strcpy\n#include <cstdlib>  // For malloc, free\n#include <expected> // For std::expected\n#include <utility>  // For std::move\n\n// Referenced types:\n// This struct definition is provided and must be used as-is.\ntypedef struct {\n    int id;\n    char* raw_data;\n    size_t len;\n} LegacyRecord;\n\n// Custom deleter for LegacyRecord.\n// It handles freeing both the internal `raw_data` (which is malloc'd)\n// and the `LegacyRecord` object itself (which is also malloc'd, to\n// preserve the original allocation mechanism for the struct).\nstruct LegacyRecordDeleter {\n    void operator()(LegacyRecord* rec) const noexcept {\n        if (rec) {\n            std::free(rec->raw_data); // Free the internal char* buffer\n            std::free(rec);           // Free the LegacyRecord object itself\n        }\n    }\n};\n\n// Modernized function to load a record.\n// It returns a std::expected which on success contains a std::unique_ptr\n// managing the LegacyRecord, and on failure contains an integer error code.\nstd::expected<std::unique_ptr<LegacyRecord, LegacyRecordDeleter>, int> load_record(int id) {\n    if (id <= 0) {\n        return std::unexpected{-1}; // Error code: Invalid ID\n    }\n\n    // Allocate LegacyRecord using malloc, then wrap it in std::unique_ptr.\n    // This preserves the original malloc-based allocation for the struct itself,\n    // while providing modern RAII management.\n    LegacyRecord* raw_rec_ptr = static_cast<LegacyRecord*>(std::malloc(sizeof(LegacyRecord)));\n    if (!raw_rec_ptr) {\n        // Malloc failed for the LegacyRecord object.\n        return std::unexpected{-2}; // Error code: LegacyRecord allocation failed\n    }\n\n    // Initialize members of the newly allocated LegacyRecord.\n    // Set raw_data to nullptr for safety in case of subsequent allocation failure.\n    raw_rec_ptr->id = id;\n    raw_rec_ptr->raw_data = nullptr; \n    raw_rec_ptr->len = 0;\n\n    // Now manage this raw pointer with std::unique_ptr and our custom deleter.\n    std::unique_ptr<LegacyRecord, LegacyRecordDeleter> record_ptr(raw_rec_ptr);\n    \n    // Use std::string for the dummy data for safer string handling.\n    const std::string dummy_data = \"DEVICE_DATA_STREAM_0xCF\";\n    record_ptr->len = dummy_data.length();\n    \n    // Allocate raw_data using malloc as per the original struct definition.\n    record_ptr->raw_data = static_cast<char*>(std::malloc(record_ptr->len + 1));\n    if (!record_ptr->raw_data) {\n        // Malloc failed for raw_data. The unique_ptr `record_ptr` will be\n        // automatically destroyed when it goes out of scope, and its deleter\n        // will be called. The deleter is robust and will correctly call\n        // `std::free(nullptr)` for `record_ptr->raw_data` (as it's still nullptr)\n        // and then `std::free(record_ptr)` for the struct itself.\n        return std::unexpected{-3}; // Error code: raw_data allocation failed\n    }\n    \n    // Copy the string data using C-style function as raw_data is char*.\n    std::strcpy(record_ptr->raw_data, dummy_data.c_str());\n    \n    // Return the successfully created and managed LegacyRecord.\n    return record_ptr;\n}",
  "369ab987671653d0d6a3f6e2c82e05e8751b03ccc530fdf9730d6d378d359d88": "#include <memory>      // For std::unique_ptr\n#include <string_view> // For std::string_view\n#include <expected>    // For std::expected, std::unexpected\n#include <utility>     // For std::move\n#include <algorithm>   // For std::copy\n\n// Referenced types:\n// This struct definition is necessary for the modernized function to compile.\n// It is assumed to be provided externally or defined once in the compilation unit.\ntypedef struct {\n    int id;\n    char* raw_data;\n    size_t len;\n} LegacyRecord;\n\n// Custom deleter for std::unique_ptr<LegacyRecord>.\n// It is responsible for freeing both the LegacyRecord itself and its\n// internal char* raw_data, assuming both were allocated with malloc.\nstruct LegacyRecordDeleter {\n    void operator()(LegacyRecord* rec) const noexcept {\n        if (rec) {\n            // Free raw_data if it was allocated\n            if (rec->raw_data) {\n                free(rec->raw_data);\n            }\n            // Free the LegacyRecord itself\n            free(rec);\n        }\n    }\n};\n\n// Modernized function to load a record.\n// It returns a std::expected containing a std::unique_ptr<LegacyRecord, LegacyRecordDeleter>\n// on success, or an int error code on failure.\nstd::expected<std::unique_ptr<LegacyRecord, LegacyRecordDeleter>, int> load_record(int id) {\n    if (id <= 0) {\n        return std::unexpected{-1}; // Error: Invalid ID (original error code)\n    }\n\n    // Allocate LegacyRecord using malloc, consistent with the original function's allocation.\n    // Then, wrap it in a std::unique_ptr with a custom deleter to manage its lifetime.\n    LegacyRecord* raw_rec_ptr = static_cast<LegacyRecord*>(malloc(sizeof(LegacyRecord)));\n    if (!raw_rec_ptr) {\n        return std::unexpected{-2}; // Error: Failed to allocate LegacyRecord\n    }\n    // std::unique_ptr takes ownership of raw_rec_ptr. If any subsequent operation fails,\n    // this unique_ptr will ensure raw_rec_ptr is properly freed.\n    std::unique_ptr<LegacyRecord, LegacyRecordDeleter> rec_ptr(raw_rec_ptr);\n\n    rec_ptr->id = id;\n\n    // Use std::string_view for the dummy data for safety and efficiency.\n    std::string_view dummy_data = \"DEVICE_DATA_STREAM_0xCF\";\n    rec_ptr->len = dummy_data.length();\n\n    // Allocate the raw_data buffer using malloc, consistent with the original.\n    // +1 for the null terminator.\n    char* raw_buffer = static_cast<char*>(malloc(rec_ptr->len + 1));\n    if (!raw_buffer) {\n        // The rec_ptr (unique_ptr for LegacyRecord) will be automatically freed\n        // when it goes out of scope, thanks to LegacyRecordDeleter.\n        return std::unexpected{-3}; // Error: Failed to allocate raw_data buffer\n    }\n\n    // Copy the string data using std::copy and ensure null-termination.\n    // std::ranges::copy could also be used here for C++20+.\n    std::copy(dummy_data.begin(), dummy_data.end(), raw_buffer);\n    raw_buffer[rec_ptr->len] = '\\0'; // Explicitly null-terminate the C-style string\n\n    rec_ptr->raw_data = raw_buffer;\n\n    // Return the unique_ptr. It will be implicitly converted to std::expected<...>\n    // indicating success.\n    return rec_ptr;\n}"
}